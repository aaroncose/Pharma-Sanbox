# Dependencias declaradas con cota inferior.
# El conjunto exacto instalado y reproducible está en `requirements.lock.txt`,
# generado con `pip freeze`. Instalar desde el lock en cualquier entorno que no
# sea de desarrollo.

# ── Web ──────────────────────────────────────────────────────────────────────
fastapi>=0.121
uvicorn[standard]>=0.41
python-multipart>=0.0.20

# ── Configuración y validación ───────────────────────────────────────────────
pydantic>=2.12
pydantic-settings>=2.13
email-validator>=2.3

# ── Base de datos ────────────────────────────────────────────────────────────
sqlalchemy>=2.0.44
psycopg[binary,pool]>=3.2.12
pgvector>=0.4.2
alembic>=1.17

# ── Seguridad ────────────────────────────────────────────────────────────────
pyjwt>=2.12
argon2-cffi>=25.1
cryptography>=46.0

# ── Infra ────────────────────────────────────────────────────────────────────
redis>=6.5
httpx>=0.28

# ── IA ───────────────────────────────────────────────────────────────────────
anthropic>=0.75

# ── Observabilidad ───────────────────────────────────────────────────────────
structlog>=25.6

# ── Desarrollo y pruebas ─────────────────────────────────────────────────────
pytest>=9.0
pytest-asyncio>=1.3
ruff>=0.15
